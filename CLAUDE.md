# EDCBViewer — Claude Code 作業ルール

## プロジェクト概要
EDCBのEPGデータ（MySQL）を参照するメディアファイルビューア（WPF / C#）。
OsqlEditのMySQL版開発と並行して作業することがある。

---

## 作業開始チェックリスト（毎回必須）

1. `git status` でクリーンなことを確認
2. 作業内容に応じたブランチを切る（main に直接コミット禁止）
3. 変更予定のファイル一覧を列挙して、承認を得てから作業開始

ブランチ命名規則：
- `feature/` — 新機能
- `fix/` — バグ修正
- `docs/` — ドキュメントのみ
- `refactor/` — リファクタリングのみ

---

## 絶対禁止事項

- **指示されたスコープ外のファイルを変更しない**
- **ドキュメント更新の指示でコードを変えない**
- **リファクタリングの指示で未使用の設定・機能を有効化しない**
- **main ブランチへの直接コミット禁止**
- **「実装を確認した」と報告する際は、該当コードを必ず貼ること。貼れないなら確認したと言わない**

---

## コード変更時のルール

- 変更前に該当コードを実際に読み、内容を貼って確認を取る
- 「問題なし」と報告する場合は根拠となるコードを示す
- スコープ外で気になる点があれば「提案」として報告するだけにとどめ、勝手に修正しない
- 複数リポジトリにまたがる作業は、1リポジトリずつ順番に行う

---

## ビルド時のルール

- `dotnet build -c Release` が EDCBViewer.exe のロックエラーで失敗した場合、`Stop-Process -Name EDCBViewer -Force` でプロセスを強制終了してからビルドを再実行する（確認不要）

---

## Git ルール

- コミット前に `git diff --stat` を提示して承認を得る
- コミットメッセージは日本語でよい
- ロールバック指示は1リポジトリずつ確実に実行し、完了後に `git status` を貼って報告

---

## 技術スタック

- WPF / C# (.NET)
- MySQL（EPGデータ参照）
- 非同期処理：`async/await` を使用（UI スレッドブロック禁止）

---

## よくあるデグレパターン（過去に発生）

- コンストラクタでの同期DB接続 → UI フリーズ（`_recordingIndex.Load()` 問題）
- 選択解除時に `_selectedMediaFile = null` を忘れる → 古いデータが書き戻される
- リファクタリング時に削除済みタブ（録画・予約）関連コードを復活させる
